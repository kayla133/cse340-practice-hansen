<%- include('partials/header') %>
<main>
    <h1>Faculty Directory</h1>
    <p>Browse our instructors and click on any instructer to see detailed information including their department and title.</p>

    <div class="faculty-list">
        <% faculty.forEach(instructor => { %>
            <div class="faculty-card">
                <h3>
                    <a href="/faculty/<%= instructor.id %>"><%= instructor.name %></a>
                </h3>
                <p class="faculty-title"><strong>Title:</strong> <%= instructor.title %></p>
                <p class="faculty-department"><strong>Department:</strong> <%= instructor.department %></p>
                <p
        <% }); %>
    </div>
</main>
<%- include('partials/footer') %>